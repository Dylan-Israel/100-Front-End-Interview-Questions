// function addAll(values: number[]) {
//   return values.reduce((total: number, current: number) => total + current, 0);
// }

// console.log(addAll(5));

let example: number = 5;
example = 'someString';
